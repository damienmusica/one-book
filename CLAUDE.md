# 하나의 책

모든 책을 품은 하나의 책. 도감이다 — 세계문학 전부가 처음부터 지도에 있고, 읽은 것이
다음 것을 연다. 값은 우리 산출물이 아니라 읽는 사람에게 쌓인다.

## 코어

- 지어내지 않는다. 없는 것은 없다고 적는다. (데이터 주장에 한한다.)
- 존재는 공짜, 깊이는 점진. 작품이 작가보다 깊을 수 없다.
- 검토된 것만 검색엔진에 제출한다. 사람은 어디든 걸어 들어온다.
- 독자의 표시는 독자 것이다. 제3자에게 아무것도 보내지 않는다.
- 규칙을 더하려는 순간 셋을 묻는다 — 목적지 한 줄 · 상속한 가정 · 이유가 소멸한 작업.

## 하지 않는 것

책을 장소로 만들지 않는다(캔버스·지도·전역 조망). 연속일과 전체 퍼센트. 런타임·빌드·CI 의
LLM. 레포와 CI 의 비밀. 생성물을 만든 컨텍스트에서 검증하는 것.

## 어디를 보나

- 제품 `docs/one-book.md` · 게이트 `docs/one-sentence-contract.md` · 이력 `docs/history.md`
- **수치는 코드가 정본이다** — 예산·상한·주기는 `qa/verify-book.mjs`·`scripts/*.ts` 에 있고
  문서에 옮겨 적지 않는다. 검증기는 합성 위반을 넣어 실제로 잡는지 본 뒤에 믿는다.
- 대량 생성·QC 는 `agent(..., { model: 'opus' })`. 판단은 세션 모델이 직접.
- 배포는 `npx wrangler pages deploy dist --project-name=literary-planet`. Pages 는 없는
  경로에 `index.html` 을 200 으로 준다 — 제목으로 확인한다.
- `npm run release` 가 전부 돈다(typecheck · test · build · 브라우저 계약).

이 레포는 Noosphere 에서 분가했다. Noosphere 의 지침은 여기 적용되지 않는다.
