# DATA_METHODOLOGY — 문학의 행성

이 문서는 데이터가 **어떻게 만들어지고, 무엇이 검증되었으며, 무엇이 아직 아닌지**를 기록한다.
앱 안의 `/methodology` 페이지는 이 문서의 사용자용 요약이며, 수치는 앱이 데이터에서 라이브로
계산한다. 정전은 객관적 진리가 아니라 편집 가능한 지도다 — 아래는 그 편집의 규칙이다.

## 1. 범위와 선정

- 중심 범위는 20세기 세계문학. 출생연도가 아니라 주요 작품 발표·활동 시기·후대 영향으로
  판단한다. 시간층 5개(뿌리 1850–1900 / 초기 모더니즘 1890–1945 / 중기 1930–1970 /
  후기·포스트모던 1960–2000 / 21세기 후속층 1990–, 기본 꺼짐)는 의도적으로 겹친다.
  '20세기'(시간)와 '모더니즘'(운동)은 별개 필터다.
- 초기 코퍼스는 **필수 검토 목록 100명 전원**이다(제외 없음 — 마스터 프롬프트의 4개 층
  목록이 그대로 초기 코퍼스가 되도록 설계했다). 선정 기준 7항은 `/methodology` 페이지에
  공개되어 있다: 형식적 혁신 · 확인 가능한 영향 · 대표성과 내부 복잡성 · 문학권 간 매개 ·
  지속적 재독 · 배제된 전통의 복원 · 판매량/수상 비의존.
- 확장 슬레이트(발저·츠바이크·로트·흐라발·키시·레비·그로스만·긴츠부르그·가로·키로가·
  아스투리아스·푸엔테스·바르가스 요사·파스·아도니스·다르위시·파로흐자드·프라무디아·
  마하스웨타 데비·하이더·바진·라오서·선충원·모옌·찬쉐·엔도·박완서·황석영·김혜순·레싱·
  고디머·먼로·카터·플라스·바흐만·델러니·딕·매카시·파묵·무라카미)는 v2에서 같은 기준으로
  검토한다.

## 2. 생성 파이프라인 (2026-08, v0.1)

1. **레지스트리 고정** — 오케스트레이터(Claude, Fable 5 세션)가 100명의 ID·표기·계층·
   티어·배치를 확정 (`data/registry.json`).
2. **프로필 생성** — 배치 12개를 분리-컨텍스트 LLM 에이전트(자기보고 기준 Opus 5)가
   `docs/editorial-guide.md`를 구속 계약으로 작성. 정직성 계약: 모르는 사실은 `UNCERTAIN:`
   보고(창작 금지), URL 위조 금지, 찬양 금지, 자기 QC 금지(생성/검토 컨텍스트 분리).
3. **관계 생성** — 클러스터 6개, `docs/relations-guide.md` 계약. 소유권 규칙(후행 작가
   기준 분할)으로 배치 간 중복을 구조적으로 차단. documented 주장은 근거 문서 실명 의무.
4. **기계 검증** — `scripts/validate-data.ts`: Zod 스키마 + 교차 불변식(ID 유일성·끝점
   실재·자기참조 금지·좌표 범위·연도 논리·독서경로 정합·**출처 없는 직접 영향 차단**·
   근거 수준-타입 정합·역방향 중복 검사·단위 구면 검사). CI 없이도 `npm run build`가
   검증을 선행 실행한다.
5. **QC (오케스트레이터, 분리 컨텍스트)** — ① 전 작가 Wikidata 생몰년 교차확인
   (`qc:crosscheck-dates`, 로컬 네트워크 전용) ② 에이전트 자기보고 `UNCERTAIN` 전건 triage
   (`docs/qc-ledger.md`) ③ 배치당 정독 샘플, 오류 발견 시 그 배치 전수 재검토(Noosphere
   결정 (34) 미러) ④ 등록 URL 실검증. 통과한 배치가 `reviewed`로 승급된다.
   Wikidata QID는 `qc:backfill-qids`(이름+생몰년 대조, 로컬 네트워크 전용)가 한 번
   해소해 `externalIds.wikidata`로 저장한다 — 이후의 교차확인은 저장된 QID를 직접
   조회하므로 재현 가능하고 동명이인 모호성이 없다. 생성기는 QID를 기억으로 지어낼 수
   없으므로 이 필드를 직접 쓰지 않는다(도구 전용 기입).
6. **좌표 생성·동결** — `scripts/generate-layout.ts`: 관계 그래프(타입별 가중치) + 태그
   친연성(운동·시대·언어·지역 공유)로 시드 고정 구면 force 배치. 같은 데이터+시드 → 같은
   좌표(결정성 테스트 보증). 생성 후 `positions.v1.json`으로 동결, 신규 작가는 동결 좌표를
   흔들지 않고 이웃 가중 중심에 증분 배치. 전체 재계산은 `--full` + 버전 범프로만.

## 3. 근거 수준 — 절대 섞지 않는 세 가지

| 수준 | 뜻 | 요건 |
|---|---|---|
| `documented` | 서신·인터뷰·회고록·번역·강의·명시적 독서 기록 | 근거 문서 실명 + 출처 필수 |
| `scholarly_consensus` | 신뢰할 만한 2차 연구가 반복적으로 다루는 계보 | 출처 필수 |
| `editorial_inference` | 이 지도가 비교를 위해 가까이 놓은 친연성 | 점선 표시, 요약에 '직접 기록 없음' 명시 |

'카프카와 베케트가 비슷하다'는 이유로 직접 영향을 표시하지 않는다. 관계 수는 작가마다
균등하지 않으며, 관계가 없는 작가는 그 상태 그대로 표시된다(데이터 미완성의 정직한 공개).

## 4. 검토 상태 사다리

- `draft` — 생성 직후. 프로필에 "초안 (검토 전)"으로 표시.
- `reviewed` — §2-4·5의 기계 검증 + 생몰년 교차확인 + 정독 샘플링 통과. `reviewedAt` 기록.
  `externalIds.wikidata`(QID) 필수 — 표시용 라벨이 아니라 코퍼스 간 엔티티 동일성의 조인 키다.
- `verified` — 외부 편집 검증(주장 단위 인용 검증). **v1에서는 부여하지 않는다** — 절차가
  갖춰지지 않은 지위를 미리 주장하지 않는다.

## 5. 출처 규율

- 공용 레지스트리(`data/sources/core.json`): Britannica·노벨상·Poetry Foundation·
  Paris Review·대학출판 총서·한국민족문화대백과 등 12종.
- 배치별 1차 문헌: 실존 문서의 제목·맥락만 기록, **URL 금지**(위조 방지). 도메인 수준
  URL은 유지관리자가 실검증한 것만.
- 번역 제목은 출판사마다 다르다 — '유일한 공식 번역명'을 단정하지 않고 항상 원제를
  병기한다. 국내 통용 표기가 미확인인 항목은 QC 원장에 열거되어 있다.

## 6. 알려진 한계 (v0.1)

- 코퍼스 밖 인물(사르트르·파운드·리처드 라이트·그레이엄 그린·고골·김억 등)과의 documented
  관계는 기록할 수 없다 — 각 관계 배치 리포트에 결손으로 명시되어 있으며 확장 슬레이트
  선정의 근거 데이터가 된다.
- 지역·언어·젠더 분포는 서구·남성·소설 편중이 있다. 수치는 `docs/coverage-report.md`와
  `/methodology`에 공개되며, 편중 축소가 다음 확장의 명시적 목표다.
- 지리 모드는 대표 좌표 1점 표시다(경로·이동 호는 v2 후보). 해안선 없이 경위선 격자만
  그린다(외부 지형 데이터 비의존).
- UI 자동 테스트는 jsdom 한계 안에서 정직하게: 데이터·로직·컴포넌트는 vitest, WebGL
  상호작용(드래그·줌·픽킹)은 실브라우저 수동 검증으로 확인·기록한다.

## 7. 좌표 버전

| 버전 | 시드 | 상태 |
|---|---|---|
| v1.x | 20260815 | `positions.v1.json` — 파일 헤더가 정본 |

전체 재배치는 사용자의 공간 기억을 깨뜨리므로, 데이터 대규모 개편 시에만 `--full`로
수행하고 방법론 변경 기록에 남긴다.

## 8. 친연성 지형 (v1.1, 세션 #2)

친연성 모드의 대륙·섬은 실지리가 아니라 **편집 지표의 지도화**다 (`docs/terrain-art-thesis.md`).

- 면적 가중 W = tierBase(anchor 2.4 · major 1.0 · context 0.55) × (1 + 0.3·정규화 관계밀도).
  tier가 1차인 이유: 그래프 차수는 큐레이션 밀도를 추적하므로(측정: 영국-아일랜드 6.43 vs
  MENA 1.60) 이를 1차 지표로 쓰면 식민지 지도를 다시 그리게 된다.
- 필드 = 구면 vMF 커널 합(κ = ln2/(1−cos R0√W)), 육지 = F ≥ τ, 해안 야생화 = 시드 고정
  도메인-워프. 수도(작가 좌표)는 비워프 자기기여 하한으로 침수 불가(테스트 성문화).
- `data/territory.v1.json`이 동결 정본: 헤더(시드·파라미터·가중·면적)는 사람이 읽는 형식,
  `geometry` 블록은 렌더러 전용 베이크(등장방형 해안/워터라인/경계 폴리라인 + 소유자 RLE).
  **렌더러는 난수를 모른다** — 앱은 베이크만 그리고, 같은 시드는 영원히 같은 행성이다.
- 등장방형 차트의 위상: 극점을 품는 영토는 폐곡선이 아니라 winding ±1의 극점 링으로
  베이크된다(현재 북극 = ngugi-wa-thiongo, 남극 = friedrich-nietzsche).
